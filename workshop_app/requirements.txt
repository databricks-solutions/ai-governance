fastapi>=0.115
uvicorn>=0.32
databricks-sdk>=0.38
pydantic>=2.9
pyyaml>=6.0
python-multipart>=0.0.12
# PDF leave-behinds (prerequisites checklist + outcomes report). The app degrades to the
# Markdown/JSON exports if this is missing, rather than 500ing.
reportlab>=4.2
