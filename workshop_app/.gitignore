# Python
__pycache__/
*.py[cod]
.venv/
.env

# Node
node_modules/
# frontend/dist IS committed so the app deploys without Node — rebuild it after
# editing frontend/src (npm ci && npm run build).

# Local config override
config/workshop.local.yaml

# Databricks
.databricks/

# OS/IDE
.DS_Store
.vscode/
